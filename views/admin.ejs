<%- include("templates/header") %>

<div class="con conCol">

    <div>Users:</div>
    <ul>
        <% let i=0; %>
		<% for(i=0; i < users.length; i++) { %>
        <%- include('templates/user', {user: users[i]})  %> 
        <% } %> 
    </ul>

    <hr>

    <a class="btn btn-primary" href="/members" role="button">Back to Members</a>

</div>

<%- include("templates/footer") %>